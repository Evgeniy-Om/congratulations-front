export type LoginFormInputsTypes = {
    email: string
    password: string
    rememberMe: boolean | undefined
}

export type ReactHookFormTextFieldTypes = {
    name: string
    type: string
    label: string
}